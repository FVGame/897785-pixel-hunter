const calc = (answers, leaves) => {
  if (!answers || typeof answers !== `object` || !leaves || typeof leaves !== `number`) {
    return false;
  }
  let points = 0;
  return points;
};

export default calc;
